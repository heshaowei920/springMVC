<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="role">



	<insert id="insertRole" parameterType="Role" useGeneratedKeys="true" keyProperty="theID">
		insert into foo.user(userID,name,age,phone,passWord) values(#{userID},#{name},#{age},#{phone},#{passWord});
	</insert>
	
	<delete id="deleteRole" parameterType="long">
		delete from role where theID = #{theID}
	</delete>
	
	<select id="getRoleByRoleID" parameterType="Long" resultType="Role">
	 select * from role where theID=#{theID}
	</select>
	
	
	<update id="updateRole" parameterType="Role">
		update role set 
		<trim suffixOverrides=",">
			<if test="name!=null">name=#{name}, </if>
			<if test="phone!=null">phone=#{phone}, </if>
			<if test="age!=null">age=#{age}, </if>
			<if test="passwd!=null">passWord=#{passWord}</if>
		</trim>
		<where>theID=#{theID}</where>
	</update>
	
	<select id="getAllRole" resultType="Role" parameterType="map">
		select * from Role 
		<trim suffixOverrides="and">
			<where>
				<if test="name!=null"> name like #{name} and </if>
				<if test="uid!=null"> userID like #{userID} and </if>
				<if test="phone!=null"> phone like #{phone} and </if>
				<if test="age!=null"> age = #{age}</if>
			</where>
		</trim>
		<if test="page!=null and pageCount!=null">
			limit #{page}, #{pageCount}
		</if>
	</select>
	
	
	<select id="getUser" resultType="User" parameterType="long">
		select * from user
		<where>theID=#{theID}</where>
	</select>
	
</mapper>
